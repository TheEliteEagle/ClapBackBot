server {
    listen 5173;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /comeback {
        proxy_pass http://backend:5000/comeback;
        proxy_set_header Host $host;
    }
}