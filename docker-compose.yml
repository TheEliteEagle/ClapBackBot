services:
  
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile-frontend
    ports:
      - "5173:5173"
  
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile-backend
    env_file:
      - .env.local
    ports:
      - "5000:5000"