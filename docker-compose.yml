services:
  
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile-frontend
    ports:
      - "5000:5000"
  
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile-backend
    ports:
      - "5173:5173"